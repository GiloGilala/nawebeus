node_modules/
dist/
.env
*.db
# Migration history is committed (NWB-P0-005); only drizzle-tooling scratch hides.
drizzle/*
!drizzle/migrations/
!drizzle/README.md
data/

# `.scratch/` is a work queue read by agents — status metadata is useless if it
# only exists locally. Track the markdown (specs + issues); ignore everything else.
.scratch/**
!.scratch/**/
!.scratch/**/*.md

# Coverage reports (generated by `bun run coverage`)
coverage/
